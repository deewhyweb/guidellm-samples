apiVersion: batch/v1
kind: Job
metadata:
  name: guidellm-benchmark-vllm
  namespace: gpt-oss-20b-benchmarks
spec:
  ttlSecondsAfterFinished: 86400
  template:
    spec:
      restartPolicy: Never
      containers:
        - name: guidellm
          image: ghcr.io/vllm-project/guidellm:latest
          env:
            - name: GUIDELLM_TARGET
              value: "http://gpt-oss-20b-vllm-lb.gpt-oss-20b-benchmark.svc.cluster.local:8000/v1"
            - name: GUIDELLM_PROFILE
              value: "concurrent"
            - name: GUIDELLM_RATE
              value: "32,64"
            - name: GUIDELLM_OUTPUTS
              value: "json,csv"
            - name: GUIDELLM_MAX_SECONDS
              value: "3000"
            - name: GUIDELLM_DATA
              value: "/data/prompts.csv"
            - name: GUIDELLM_PROCESSOR
              value: "gpt-oss-20b"
            - name: HF_HOME
              value: "/cache"
          volumeMounts:
            - name: data
              mountPath: /data
              readOnly: true
            - name: cache
              mountPath: /cache
            - name: results
              mountPath: /results
      volumes:
        - name: data
          persistentVolumeClaim:
            claimName: benchmark-data
        - name: cache
          emptyDir: {}
        - name: results
          emptyDir: {}
